[build]
buildCommand = "pnpm --filter @crm/shared-types build && pnpm --filter @crm/web build"

[deploy]
startCommand = "cd apps/web && npx next start -p ${PORT:-3000} -H 0.0.0.0"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
